<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/clear_history_view"
    android:layout_width="match_parent"
    android:layout_height="@dimen/history_item_height"
    android:background="@color/white">

    <TextView
        android:id="@+id/clearhistory"
        android:layout_width="match_parent"
        android:layout_height="@dimen/route_history_img_layout_height"
        android:layout_gravity="center"
        android:background="@drawable/mz_item_background"
        android:gravity="center"
        android:text="@string/clear_history"
        android:textColor="@color/gray"
        android:textSize="@dimen/search_history_title_text_size" />
</FrameLayout>